1. 如果mcp-server没有变绿，复制命令到cmd执行，看看是否有报错
2. 使用npm install -g 下载对应moudle
3. 比如cmd /c npx -y @smithery/cli@latest run @wopal/mcp-server-hotnews --client cursor --config "{}"
   1. 就使用`npm install -g @wopal/mcp-server-hotnews `
